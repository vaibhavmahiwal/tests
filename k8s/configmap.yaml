apiVersion: v1
kind: ConfigMap
metadata:
  name: blog-api-config
  namespace: default
data:
  NODE_ENV: "production"
  PORT: "3000"
  REDIS_URL: "redis://<redis-endpoint>:6379"
  CORS_ORIGIN: "https://yourblog.com"

